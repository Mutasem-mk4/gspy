name: "CodeQL"

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  schedule:
    - cron: '21 3 * * 0'

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    
    strategy:
      fail-fast: false
      matrix:
        language: [ 'go' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

    - name: Initialize CodeQL
      uses: github/codeql-action/init@d1d73aca0d59d21434cf0e464ed3a86b080a5ecf # v3.25.0
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@d1d73aca0d59d21434cf0e464ed3a86b080a5ecf # v3.25.0

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@d1d73aca0d59d21434cf0e464ed3a86b080a5ecf # v3.25.0
      with:
        category: "/language:${{matrix.language}}"
